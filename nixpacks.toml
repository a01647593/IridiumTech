[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
    "cd ai-learning",
    "rm -rf node_modules package-lock.json",
    "npm install"
]

[phases.build]
cmds = [
    "cd ai-learning",
    "npm run build"
]

[start]
cmd = "cd ai-learning && npm run preview -- --host 0.0.0.0 --port 3000"